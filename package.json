{
  "name": "root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "7.4.2"
  }
}
